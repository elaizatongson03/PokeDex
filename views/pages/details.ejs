<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("../partials/header") %>
  </head>
  <body class="bg-gray-200 text-gray-800 flex flex-col items-center justify-center min-h-screen relative">
    <div class="bg-red-700 border-8 border-red-900 rounded-lg shadow-2xl p-6 w-[600px] h-[400px] relative flex flex-row">
      <!-- Left Panel -->
      <div class="w-1/2 relative p-3 border-r-4 border-red-900 flex flex-col items-center">
        <div class="absolute top-2 left-2 flex space-x-2">
          <div class="w-6 h-6 bg-blue-500 border-2 border-gray-800 rounded-full"></div>
          <div class="w-4 h-4 bg-green-500 border-2 border-gray-800 rounded-full"></div>
          <div class="w-4 h-4 bg-yellow-500 border-2 border-gray-800 rounded-full"></div>
        </div>
        <div class="bg-white border-4 border-gray-800 rounded-lg p-3 flex flex-col items-center w-full">
          <img src="<%= pokemon.sprites.front_default %>" alt="<%= pokemon.name %>" class="w-32 h-32" />
        </div>
        <div class="mt-2 flex justify-center">
          <div class="w-4 h-4 bg-black rounded-full mx-1"></div>
          <div class="w-8 h-4 bg-blue-500 border-2 border-gray-800 rounded-md"></div>
        </div>
      </div>
      
      <!-- Right Panel -->
      <div class="w-1/2 p-3 flex flex-col justify-center">
        <h1 class="text-xl font-bold text-center capitalize text-white">
          <%= pokemon.name %>
        </h1>
        <div class="mt-2 p-3 bg-gray-100 border border-gray-800 rounded-lg w-full">
          <p class="text-lg font-semibold">Height: <%= pokemon.height %></p>
          <p class="text-lg font-semibold">Weight: <%= pokemon.weight %></p>
          <p class="text-lg font-semibold">Type: <%= pokemon.types.map(t => t.type.name).join(', ') %></p>
        </div>
    
        <div class="text-center mt-5">
          <a href="/" class="px-4 py-2 bg-red-500 text-white font-bold rounded-lg shadow-md hover:bg-red-700 transition">Back</a>
        </div>
      </div>
    </div>
    <%- include("../partials/footer") %>
  </body>
</html>
